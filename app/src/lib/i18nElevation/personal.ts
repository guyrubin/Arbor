/* i18nElevation/personal — E6a strings: avatar presence on parent surfaces
 * (ChildProfile child card, WeeklyTab "{name}'s week" header).
 *
 * CLINICAL FIREWALL: nothing here annotates child data — presentation labels
 * only, no counts needed, and never %/verdicts/deltas. Hebrew = calm
 * Israeli-parent transcreation, outcome language, no AI/tech framing;
 * flagged for arbor-localization native review. */

export const en: Record<string, string> = {
  // ── E6a · Weekly report header — section kicker above "{name}'s week".
  //    Same wording as the "My Child" section the page lives under, so the
  //    header reads as part of one place, not a new concept.
  "elev.personal.weekly.eyebrow": "My Child",
};

export const he: Record<string, string> = {
  "elev.personal.weekly.eyebrow": "הילד שלי",
};
