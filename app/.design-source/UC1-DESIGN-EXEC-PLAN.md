# UC-1 Design — EXECUTION PLAN (leveled, 2026-06-29)

Companion to `UC1-DESIGN-RECONCILE-PLAN.md` (per-screen detail). This doc = the build sequence, shared-file lock order, acceptance criteria, capability-spec obligations, and the 3-session coordination contract. Branch `claude/uc1-design-impl`.


## Build waves


### Wave 0 — Pre-flight green baseline (serial, 1 agent, ~5 min)
_Establish the green baseline on claude/uc1-design-impl BEFORE any edit, so every later gate compares against a known-good state. Confirm working tree clean, run the full gate cold, and snapshot the 45-route registry + navigation.test.ts expectations so capability-floor regressions are detectable by diff._

**Agents:**
- pre-flight-baseline (single agent): run npm ci if needed; npm run lint && npm run test && npm run build; capture pass/fail; dump the ActiveTab union and SECTIONS/TAB_SECTION_FALLBACK route list to a scratch file as the 45-route floor manifest.

**GATE:** GREEN BASELINE: npm run lint (tsc --noEmit) + npm run test (vitest) + npm run build all pass on an unmodified tree. If red on arrival, STOP and report — do not build on a red baseline. Route-floor manifest captured.

### Wave 1 — FOUNDATION / shared substrate (SERIAL, one agent owns the whole wave)
_Land every shared-file change ONCE, in lock order, so the parallel wave consumes a stable substrate. Scope: (a) shared primitives in ui/kit.tsx — RadialProgress/ProgressBar, 54px rounded-15 InitialsTile, label/value InsetRow, two-pane Split, generalized Badge slot, and a single domain->{lucide icon, layout-kit tone} map (consumed by Growth/Academy/Journal/Care); (b) navigation.ts IA reshape to the 8 design categories preserving all 45 leaves + subTabsForSection(Overview-first) + generalized NavSection badge + TAB_SECTION_FALLBACK update + register 'journal'; (c) ArborContext.tsx — add 'journal' to ActiveTab union, expose read-only unread-coach badge count; (d) i18n.ts — add ALL en+he key scaffolds for every screen (nav.title.*/nav.sub.*/nav.short.*, category labels, settings-popover, today.meta, devscore.stat.*, beh.* hero/stats/mode/domain/devMap, ms.mapHint/allDomains/developmentMap, journal.*, foryou/academy headings, consult 'Your summary'/prosTitle) so leaf agents only consume keys, never add them; (e) index.css — append ONE new sapphire-band + navy sub-tab-pill --arbor-* token AFTER the existing .arbor-parent block (never touch :root --arbor-clay, never touch .arbor-play L448-564); (f) Shell/Sidebar/Topbar/MobileNav/TopbarSearch/TopbarKidSwitcher/ChildContextHeader shell+IA refactor editing only BETWEEN the immovable KidMode anchors (Provider L180/L344, KidModeButton compact L249, Overlay L342) and keeping KidModeOverlay OUTSIDE <main className=arbor-parent>; register journal:JournalTab in Shell route map; (g) navigation.test.ts — update My-Child expected tab list to include 'journal' + add the guard test asserting sectionForTab() resolves for ALL ActiveTab values._

**Agents:**
- foundation (single serial agent, edits in lock order): ui/kit.tsx (+tokens.ts read-only) -> i18n.ts scaffold -> index.css one-token append -> navigation.ts -> ArborContext.tsx -> Shell.tsx/Sidebar.tsx/Topbar.tsx/MobileNav.tsx/TopbarSearch.tsx/TopbarKidSwitcher.tsx/ChildContextHeader.tsx -> navigation.test.ts. No leaf/tab-content work in this wave.

**GATE:** FOUNDATION GREEN: npm run lint + npm run test + npm run build pass. Capability-floor: navigation guard test proves all 45 ActiveTab values resolve to a section; nav.test My-Child list updated and passing. Anchor check: Shell.tsx still has KidModeProvider/KidModeButton compact/KidModeOverlay mounted with Overlay OUTSIDE arbor-parent main. Firewall check: no 0-100 score / verdict tag / weakest-domain pointer introduced in shell. i18n: every new key present in BOTH en+he (no raw-key render). COMMIT this wave as the substrate tag before fan-out.

### Wave 2 — PARALLEL per-screen content (fan-out, disjoint tab/section files only)
_Build the 7 remaining parent surfaces in parallel, each agent editing ONLY its own disjoint tab/section components and CONSUMING the Wave-1 substrate (kit primitives, i18n keys, nav helpers, domain map). No agent edits any shared file. Each folds in the capability-spec obligations for its screen: kid-originated events surfaced in Today/Profile feeds (Loops 1+3), assign-activity->kid-quest affordance + 'feeds Map/Academy/Care' hint on Growth/Academy (Loop 2), Behaviors navy 'Linked to Development Map' card feeding Self-Regulation + Journal + Coach, Journal AUTO-vs-MANUAL provenance badge + per-entry 7-domain chip reading shared profile (Loop 5), Profile child-card avatar rendered via the shared HeroAvatar engine + live ShareGrants Family Circle + multi-child switcher + kid-activity tracker (Loop 4), Care redactable consult packet built from live child record with section labels mirroring the 7 domains, Ask Arbor data-personalized coach + append-only memory queue. Each agent honors the clinical firewall (count-based non-verdict stats only) and RTL/i18n._

**Agents:**
- today-agent: OverviewTab.tsx + overview/* + sections/DevScoreCard|DayWindowsPanel|ArborNoticedCard (read-only) — hero meta row, count-based 3-stat dev footer, generalized ActivityRow feed INCLUDING kid mood/quest/star events from shared profile, coach card. NO % ring.
- growth-agent: MilestonesTab.tsx + DevelopmentTab.tsx + Strengths.tsx — two-pane master/detail, 7 domain rows w/ count bars (never 0-100), domain drill-in, 'feeds Map/Academy/Care' + assign-activity->quest affordance. Reuses kit Split/ProgressBar/domain-map. Binary checked semantics preserved (no tri-state verdict).
- behaviors-agent: BehaviorsTab.tsx + behaviors/PatternInsights.tsx — coral hero + flat-count 3-stat strip, quick-log tiles, expandable event rows w/ 5-dot meter, navy 'Linked to Development Map' card wired to setActiveTab('growth') and feeding Self-Regulation/Journal/Coach. Thread t() into PatternInsights.
- journal-agent: NEW JournalTab.tsx + JournalRow + reuse signalTimeline — compose card (Voice/Photo/Text), flat feed reading buildTimeline, AUTO/MANUAL provenance badge, per-entry 7-domain chip, kid-side auto-logged moments. Does NOT replace StoryTimelineTab. Consumes ActiveTab 'journal' + route already wired in Wave 1.
- academy-agent: Masterclasses.tsx + AcademyForYou.tsx + ScholarHubCard.tsx + ScholarTab.tsx — Learning Map rail (ring via --arbor-green-ink) + 7-domain bars matched to gaps, gallery course cards w/ PASTEL header bands, binary done=0/100 progress only. Reuses kit RadialProgress/domain-map.
- care-agent: AskSpecialist.tsx + FindProfessional.tsx + CareTeam.tsx + Appointments.tsx + TrustedSharing.tsx — two-column Consult (live packet left / verified-pros rail right), packet section labels mirror 7 domains, 54px InitialsTile from kit, green tokens not blue. Live data plumbing preserved (no static design arrays).
- profile-ask-agent: Profile/child-card + ProfileTab + coach/AskArbor surface — child-card avatar via shared HeroAvatar engine (Loop 4, never re-composite), real kid+parent activity feed, Family Circle from live ShareGrants, multi-child switcher + kid-activity tracker + Arbor Plus; Ask Arbor stays data-personalized w/ append-only memory review queue. Must NOT restyle HeroAvatar/ArborMascot internals (kid-owned).

**GATE:** PER-SCREEN GREEN (run after each agent + once for the merged set): npm run lint + npm run test + npm run build pass. Each screen meets its perScreenAcceptance. Firewall scan: no 0-100 score, verdict tag, on-track/keep-an-eye, or weakest-domain pointer on any child-data surface. i18n: en+he parity for every consumed key; RTL flips as layout. Kid-contract scan: no parent edits under kidmode/**, KidModeButton.tsx, playkit.tsx, ArborMascot/HeroAvatar internals, or the .arbor-play/--play-*/--comic-* zone; no kid route added to SECTIONS/ActiveTab.

### Wave 3 — VERIFY / capability-floor + zero-regression review (serial gate)
_Prove zero capability regression and full blueprint-contract coverage across the integrated branch before declaring UC-1 done. Run the full gate, the 45-route floor diff vs the Wave-0 manifest, the clinical-firewall sweep, the connected-system contract checklist (5 loops + journal provenance + dev-map spine + coach personalization + kid-mode embed preserved), the kid/visual coordination scan, and a per-screen visual-vs-mock + RTL/he pass._

**Agents:**
- verify-agent (serial): npm run lint + npm run test + npm run build; diff live ActiveTab/SECTIONS against Wave-0 route-floor manifest (all 45 reachable); grep firewall sweep (no %/verdict/deficit on child data); confirm Loop 1-5 affordances present (kid events in Today/Profile feed, assign->quest, navy DevMap card, journal AUTO/MANUAL+domain chip, HeroAvatar-everywhere, shareable growth output); confirm KidMode anchors intact + Overlay outside arbor-parent; confirm index.css :root --arbor-clay unchanged and .arbor-play zone untouched; he/RTL render check per screen.

**GATE:** RELEASE-CANDIDATE GREEN: full gate passes; 45-route floor intact (zero orphaned leaves); clinical firewall clean; all 11 connected-system contracts honored or explicitly deferred-with-reason; kid/visual coordination scan clean (no parent edits in kid-owned zones, no shared-token drift); en+he+RTL verified per screen. Build to green and STOP — prod-promote (merge to main) is Guy's Level-3 gate, never auto-deployed.

## Shared-file lock order (foundation wave, serial)

- 1. src/components/ui/kit.tsx (+ src/lib/tokens.ts READ-ONLY mirror) — OWNER: foundation agent. Add shared primitives first (RadialProgress, ProgressBar, 54px InitialsTile, label/value InsetRow, two-pane Split, generalized Badge slot) + the single domain->{lucide icon, layout-kit tone} map. Never alter PlayTone/TONE_INK/TONE_SOFT/BRAND_CONFETTI (kid-owned). Everything downstream imports from here, so it lands first.
- 2. src/lib/i18n.ts — OWNER: foundation agent. Append ALL en+he key scaffolds for every screen at the END of each existing namespace cluster (never interleave/reorder). Leaf agents in Wave 2 only CONSUME keys; they never add to i18n.ts. This removes i18n as a Wave-2 conflict source entirely.
- 3. src/index.css — OWNER: foundation agent. Append exactly ONE new --arbor-* token (sapphire topbar band + navy sub-tab pill) AFTER the existing .arbor-parent block (serial-append discipline). DO NOT touch :root --arbor-clay (locked sapphire), DO NOT touch the .arbor-play / --play-* / --comic-* zone (lines ~448-564, kid-owned), DO NOT touch the line-1 font @import. No whole-file reflow.
- 4. src/lib/navigation.ts — OWNER: foundation agent. SECTIONS reshape to 8 categories preserving all 45 leaves under items[]; add subTabsForSection (Overview-first); generalize NavSection badge; update TAB_SECTION_FALLBACK; register 'journal'. IA spine consumed by Sidebar/Topbar/MobileNav/Shell/SearchModal — must be stable before fan-out.
- 5. src/context/ArborContext.tsx — OWNER: foundation agent. Add 'journal' to the ActiveTab union (L58) and expose a read-only unread-coach badge count. Touched by SearchModal/navigation/JourneyTab, so it must settle in the serial wave.
- 6. src/components/layout/Shell.tsx (+ Sidebar/Topbar/MobileNav/TopbarSearch/TopbarKidSwitcher/ChildContextHeader) — OWNER: foundation agent. Edit ONLY between the immovable KidMode anchors: KidModeProvider open L180, KidModeButton compact mount L249, KidModeOverlay sibling L342, KidModeProvider close L344; keep KidModeOverlay OUTSIDE <main className=arbor-parent> (L199). Register journal:JournalTab in the route map. Do not delete/nest/reorder the 4 anchor lines.
- 7. src/lib/navigation.test.ts — OWNER: foundation agent. Update My-Child expected tab list to include 'journal'; add the guard test asserting sectionForTab() resolves for every ActiveTab value (the 45-route floor enforcer). Last in the foundation lock chain so it tests the final IA.

## Parallelization map

- SERIAL (must precede everything): Wave 0 baseline, then Wave 1 foundation — both single-agent because they touch the shared substrate (kit.tsx, i18n.ts, index.css, navigation.ts, ArborContext.tsx, Shell+layout, navigation.test.ts). No screen content runs until foundation commits green.
- PARALLEL (Wave 2 — disjoint file sets, safe to run concurrently): today-agent (OverviewTab + overview/* + DevScoreCard/DayWindowsPanel/ArborNoticedCard read-only) | growth-agent (MilestonesTab + DevelopmentTab + Strengths) | behaviors-agent (BehaviorsTab + behaviors/PatternInsights) | journal-agent (NEW JournalTab + JournalRow) | academy-agent (Masterclasses + AcademyForYou + ScholarHubCard + ScholarTab) | care-agent (AskSpecialist + FindProfessional + CareTeam + Appointments + TrustedSharing) | profile-ask-agent (Profile/child-card + ProfileTab + coach/AskArbor surface). These seven sets are file-disjoint and all consume the Wave-1 substrate read-only.
- SHARED-FILE OVERLAP FORCING SERIALIZATION (why Wave 2 cannot touch these): kit.tsx, i18n.ts, index.css, navigation.ts, ArborContext.tsx, Shell.tsx + layout/*, navigation.test.ts are all edited ONCE in Wave 1; any Wave-2 need for them is pre-satisfied by the foundation agent so leaf agents never open them.
- WATCH — DevScoreCard.tsx is read/reused by BOTH today-agent (Today dev footer) and growth-agent (left-rail Map summary). Treat as shared: foundation agent freezes its firewalled count-headline contract; the two consumers only wrap/position it, neither edits its internals. If internal edits are needed, route through foundation, not in parallel.
- WATCH — signalTimeline lib is read by journal-agent (new JournalTab) and the existing StoryTimelineTab; journal-agent reuses buildTimeline READ-only and must not modify the shared engine. HeroAvatar/ArborMascot are kid-owned — profile-ask-agent renders THROUGH them but never edits their internals.
- SERIAL (Wave 3 verify): single verify-agent gate after the parallel set integrates.

## Per-screen acceptance criteria

- SHELL/IA: Visual — 8-category sidebar + Overview-first navy/white sub-tab pill row + sapphire topbar band (mapped --arbor-* token, no raw hex) + title/subtitle topbar + account-row settings/lang popover. Capability — all 45 routes reachable (guard test green); Ctrl/Cmd+K search, billing-return polling, AiRail toggle, SettingsModal contents, both UI-lang and AI-lang toggles preserved; mobile keeps a language path + 'More' exposes every category. Firewall — ChildContextHeader stays presence-only (no score/verdict). Kid/i18n — KidModeProvider/Button/Overlay anchors intact, Overlay outside arbor-parent; en+he for every nav.title/sub/short/category key; RTL flips chevrons + popover anchoring.
- TODAY: Visual — guidance-briefing hero (meta row + scrim, 188px), count-based 3-stat dev footer, unified ActivityRow feed (icon-tile+title/sub+trailing status), coach card w/ online dot. Capability — hero AI-focus well + regenerate, StreakChip, DailyPlayCard full prop surface, Goal Builder, JITAI nudge, daily-tools drawer all preserved; feed surfaces KID-ORIGINATED mood/quest/star events from the shared profile (Loops 1+3), 'Live' pill tied to recent activity not faked. Firewall — 3 stats are counts only, status glyphs neutral/positive, no % ring/verdict. i18n/RTL — today.meta + devscore.stat.* in en+he, logical props throughout.
- GROWTH: Visual — two-pane master/detail (Development Map left rail + 7 domain rows w/ count bars + domain drill-in). Capability — milestone toggle+confetti, domainStats, age-band grouping, preterm control, custom-milestone CRUD, AI Explain, scaffold gap analyzer, watch-points, HeroAvatar portraits all preserved and reachable in the new layout; Loop-2 'feeds Map/Academy/Care' hint + assign-activity->kid-quest affordance present. Firewall — bars are checked/total counts only; NO 69% overall, NO per-domain %, NO on-track/keep-an-eye tag, NO tri-state mastered/emerging (binary checked kept). i18n/RTL — ms.mapHint/allDomains/developmentMap en+he, framework.json labels as source of truth, back-link chevron flips in he.
- BEHAVIORS: Visual — coral hero (mapped peach/clay tokens, no new hue) + flat-count 3-stat strip, Voice/Photo/Text quick-log tiles, expandable event rows w/ 5-dot meter + status icon, PatternInsights as icon-row list, navy 'Linked to Development Map' card. Capability — full log form, voice-to-log, quick-fill templates, edit-in-place, delete-guard, resolve toggle, inline co-reg script + Discuss-in-Coach, whole-log analysis, filters, weekly grouping, PDF export, typeCounts30d flat-count card all preserved; DevMap card wires setActiveTab('growth') AND feeds Self-Regulation/Journal/Coach. Firewall — avg-intensity is a flat current-window number (never a sparkline/trend), no verdict status. i18n/RTL — beh.* keys en+he, PatternInsights threaded through t(), 5-dot meter RTL-aware.
- JOURNAL: Visual — NEW JournalTab with top compose card (Voice/Photo/Text tiles), flat single-column feed (~840px) of rows w/ AUTO-vs-MANUAL provenance badge + per-entry 7-domain chip + right-aligned relative time. Capability — StoryTimelineTab kept fully intact (additive, not replaced); JournalTab reuses buildTimeline (same real data) and MemoryRow ledger READ-only, never forks memory-approval logic; auto entries include kid-side moments from shared profile (Loop 5); compose tiles trigger the EXISTING capture flow (no invented capture logic). Firewall — domain chips descriptive not evaluative, no intensity-verdict coloring. i18n/RTL — journal.* + domain labels en+he, relative-time labels localized, ms-auto/logical props for RTL.
- ACADEMY: Visual — two-column (Learning Map rail + 'All courses' gallery), per-domain bars w/ icon+dot+percent, gallery course cards w/ PASTEL gradient header bands + per-card progress + footer CTA chip. Capability — Reader (sections/'what to say'/'try tonight'/private reflection/mark-complete confetti), localStorage done+reflection persistence, Family-Charter recommended strip, catalog progress bar, AcademyForYou verbatim cleared copy + framing gate, ScholarHubCard, ScholarTab apply-lens/see-example coach-seeds all preserved; courses matched to Development-Map GAPS sharing the 7-domain taxonomy (Loop 2). Firewall — ring uses --arbor-green-ink (not #2b7fff), AcademyForYou stays non-ranked/non-deficit, per-course bar is binary 0/100 (no fabricated granular %). i18n/RTL — learnMap/courses headings en+he, rail order reads correctly in he.
- CARE: Visual — Consult two-column (live redactable packet card left / verified-pros rail right), label/value inset rows w/ include-toggle check, GDPR/COPPA trust row inside summary card (green tokens not blue), 54px rounded-15 InitialsTile across pro/CareTeam cards. Capability — packet built from LIVE child record (never static design arrays), per-item redaction + includedCount, four export verbs (copy/.md/PDF-popover-with-full-keyboard-nav/send-modal), FindProfessional live fetch+fallback+filters+consult transaction+embedded mode prefill, CareTeam/Appointments/TrustedSharing server-enforced logic untouched (cosmetic-only); packet section labels mirror the 7 Development-Map domains (Growth->Care threading). Firewall — guidance-not-diagnosis preserved, route-to-pro intact. i18n/RTL — 'Your summary'/prosTitle en+he, far-end pill + rail use logical props in he.
- PROFILE + ASK ARBOR: Visual — Profile child-card w/ avatar, real activity feed, Family Circle, multi-child switcher, kid-activity tracker, Arbor Plus; Ask Arbor as first-class data-personalized coach surface. Capability — child-card avatar renders through the SHARED HeroAvatar engine (Loop 4, never re-composite; do not restyle HeroAvatar/ArborMascot internals); activity feed shows real kid+parent events from shared profile; Family Circle reads live ShareGrants (role-based); coach seeded w/ child profile/lens/mood context + append-only memory review queue (ledger the growth story reads) preserved; deep-links from every surface carry seeded context. Firewall — no score/verdict on child data; on-device privacy framing kept. i18n/RTL — en+he for all consumed keys, logical props throughout.

## First command

```
cd /c/Users/dguyr/ROS/Arbor/app && git branch --show-current && npm run lint && npm run test && npm run build   # Wave 0: confirm we are on claude/uc1-design-impl and the baseline is GREEN before launching the single foundation agent (Wave 1). Do NOT launch the parallel per-screen wave until foundation commits green.
```

## AI capability-spec contracts (connected system — must be honored, not broken)

- SINGLE CHILD PROFILE = SOURCE OF TRUTH: every parent screen reads from and writes to one child profile (mood, milestones, stars, activities, avatar all live there). Parent and kid apps are 'two faces of one brain' with realtime sync. No parent screen may introduce a sibling data store or a verdict/score that isn't a read of this profile.
- LOOP 1 EMOTIONAL INSIGHT: kid mood check-in -> Parent Today feed -> emotional milestone (Self-Regulation domain) -> Coach context. The parent Today feed and Coach MUST surface kid-originated mood data so the parent 'understands without prying.'
- LOOP 2 GUIDED ACTION: Milestone/Growth flags a gap -> Coach/AI recommends -> Parent ASSIGNS an activity -> it becomes the kid's quest. There must be a parent-side 'assign activity' action that crosses into kid mode; Growth->Coach->Academy/Activity assignment is one chain.
- LOOP 3 MOTIVATION & HABIT: complete quest/comic -> earn stars+streak -> Treasure unlocks -> returns tomorrow. Parent surfaces (Today feed, Profile activity, streak chip) reflect kid stars/streak/quest completions.
- LOOP 4 HERO AVATAR EVERYWHERE: ONE saved on-device avatar rendered through a single shared canvas module across Hero Card, comic covers, 6 comic panels, Academy hero, Studio stamp, reward/badge art, breathing buddy, milestone celebration. Build rule: add a surface by passing a new template to the shared module, never re-implement compositing. Parent surfaces that show the child (Profile child-card, milestone celebrations, shareable Hero Card) must render via this same engine.
- LOOP 5 GROWTH STORY: quests+comics+moods+activities -> milestones move -> Journal auto-logs -> shareable Hero Card / Weekly Report. The growth story compounds into a SHAREABLE artifact; Journal auto-logging and the Weekly Sunday digest are the connective outputs.
- JOURNAL AUTO-LOG CONTRACT: the Journal auto-logs moments (from kid activity/mood/milestones) AND accepts manual notes; entries carry an AUTO (Arbor-generated) vs MANUAL provenance flag and a Development-Map DOMAIN tag tying each moment to one of the 7 clinical domains.
- DEVELOPMENT MAP AS THE SPINE: 7 clinical domains / 35+ milestones are the shared taxonomy threaded through Today (dev-map card), Growth (the Map), Behaviors (resolved events feed Self-Regulation), Journal (per-entry domain chip), Academy (courses matched to gaps), and Care (consult packet mirrors domains). Every cross-surface reference uses the SAME 7 framework.json domain labels.
- COACH IS DATA-PERSONALIZED: the AI coach is personalized by the child's data (milestones, mood, behavior logs, lens) and is reachable as a connective endpoint from every surface (deep-link with seeded context). It also runs the parent-approved memory review queue (append-only ledger).
- GUIDANCE-NOT-DIAGNOSIS / CLINICAL FIREWALL: Arbor is a developmental guidance tool, never diagnostic; for any concern it routes to a verified professional (Care Network). No 0-100 score, verdict tag, or weakest-domain deficit pointer may appear on child data on any parent screen.
- KID MODE IN THE WEB APP: the parent web app embeds a Kid Mode (web-app handoff exposes kidMode/kidAvatar/kidStars/kidQuest/exitKid tokens) — 'the full parent dashboard plus a Kid Mode, always in sync.' Mobile & web are at full parity (both worlds, 6 SEL games, hero comics).
- BILINGUAL/RTL & ON-DEVICE PRIVACY ARE SYSTEM-WIDE CONTRACTS: every screen, story, and game flips He/En as layout (not just text); avatar renders on-device and child data stays put until the parent exports (GDPR/COPPA).

## Per-parent-screen connected-system obligations

- TODAY: the live activity feed must surface KID-ORIGINATED events (mood check-ins, quest/comic completions, stars/streak) not just parent-logged moments — this is the Loop-1 + Loop-3 parent-facing window. The 'one daily action' hero is the entry to Loop-2 guided action. (Plan currently derives the feed only from parent playLogs/behaviorLogs/milestones — must include kid mood + quest/star events from the shared profile.)
- GROWTH (Development Map): must expose the Loop-2 chain explicitly — a gap on the Map feeds Coach recommendation feeds an ASSIGNABLE activity that becomes the kid quest. The plan adds a 'feeds the Map/Academy/Care' hint banner but omits the 'assign activity -> kid quest' affordance that closes Loop-2.
- BEHAVIORS: the 'Linked to Development Map' navy card (resolved events feed Self-Regulation) is the connective hook and is correctly planned — but it must also feed Journal auto-log and Coach context, not just deep-link to Growth.
- JOURNAL: must render the AUTO vs MANUAL provenance badge AND the per-entry 7-domain chip, and the auto entries must include kid-side moments (mood, quest, milestone crossings) auto-logged from the shared profile — this is the Loop-5 'Journal auto-logs' output. (Plan maps provenance heuristically and risks treating Journal as parent-log-only.)
- ACADEMY: courses must be matched to Development-Map GAPS (Loop-2: 'expert courses, matched to gaps') and the Learning Map must share the same 7-domain taxonomy + domain->icon/tone map used by Growth. The development copilot lives here (per deck).
- CARE NETWORK: the redactable consult packet must be built from the live child profile and its section labels must mirror the 7 Development-Map domains (Growth->Care threading); it is the 'route to a verified professional' endpoint of the guidance-not-diagnosis contract.
- PROFILE: the child-card avatar MUST render through the shared avatar canvas module (Loop-4), the activity feed must show real kid+parent events from the shared profile, and Family Circle must read live ShareGrants (role-based co-parents/carers = the deck's 'Family Circle'). Profile is also where 'multiple children' + kid-activity tracker + Arbor Plus live.
- ASK ARBOR (Coach): must remain data-personalized (seeded with child profile/lens/mood context) and keep the parent-approved memory review queue (the append-only ledger that the growth story reads). It is the hub every other surface deep-links into with context.
- SHELL/IA: Ask Arbor as a first-class category is consistent with the deck's 6-category model (Today, Growth, Academy, Ask Arbor, Care Network, Profile). The Shell must also preserve the Kid Mode entry/route (KidModeButton/KidModeOverlay) because the web app explicitly contains an in-sync Kid Mode — not merely 'out of scope wiring.'

## Competitive must-wins (do not bury)

- The HERO AVATAR ENGINE is the signature no competitor has — one on-device avatar rendered everywhere (Hero Card, comics, Academy hero, rewards, breathing buddy, milestone celebrations). The parent UI must visibly surface this (Profile child-card + milestone celebrations + shareable Hero Card) rather than treating the avatar as a static profile picture.
- ALL FOUR COMPETITOR STRENGTHS COMBINED: Kinedu milestone tracking + Lovevery stage-based activities + Khan Kids character-led learning + Duolingo streaks/reward loops — Arbor is the only one that fuses all four. The parent screens must keep milestone tracking (Growth), activity assignment (Today/Growth->quest), and the streak/reward signal (Today feed, StreakChip) all visible and connected.
- TWO SYNCED WORLDS / REALTIME SYNC: 'two faces of one brain' — the parent never sees a stale or divergent picture from the kid. Parent feeds reflecting kid actions in near-real-time (the 'Live' pill) is a differentiator the parent UI must honor honestly (tie 'Live' to recent activity, don't fake it).
- BILINGUAL HE/EN WITH FULL RTL AS LAYOUT (not just translated text) across every screen, story, and game — a first-class market differentiator for the Israeli-first GTM; must not regress in the reconciliation.
- ON-DEVICE / PRIVACY-FIRST (GDPR/COPPA): avatar renders on-device, child data stays put until the parent exports; the redactable consult packet + parent-approved memory queue are trust moats the UI must keep prominent.
- THE CONNECTED SYSTEM ITSELF: 'every feature feeds the next, value compounds' — 20+ connected features feeding ONE growth story. The compounding loops (not any single screen) are the moat; the parent UI must keep the connective-tissue affordances (cross-surface hints, assign->quest, feeds-the-Map) rather than presenting 6 isolated tools.
- THE GROWTH STORY -> SHAREABLE HERO CARD: a visible, shareable artifact of growth (Weekly Report + Hero Card) is the viral/retention differentiator; the parent UI should expose a shareable growth-story output.

## Capabilities the per-screen plan was MISSING (now folded in)

- NO KID-MODE EMBED TREATMENT: the web-app handoff exposes kidMode/kidAvatar/kidStars/kidQuest/exitKid tokens and the deck states the web app includes 'a Kid Mode, always in sync.' The plan only says 'Kid Mode is out-of-scope visually but keep the wiring/route' — it never accounts for the parent web app's in-app Kid Mode surface as a first-class connected element. This is the largest blueprint capability the per-screen plan does not address.
- AVATAR ENGINE (LOOP 4) AS A SHARED RENDER MODULE: the blueprint's build rule (one shared canvas module; add a surface via a template, never re-implement compositing) is absent. The plan reuses HeroAvatar on Profile but never names the avatar-everywhere contract or guards against per-surface re-compositing; milestone-celebration / Hero-Card / reward-art surfaces are not in scope anywhere.
- SHAREABLE HERO CARD + WEEKLY REPORT (LOOP 5 OUTPUT): the deck makes the Weekly Sunday digest (Development-Map trend, wins, one insight) and a shareable Hero Card the culmination of the growth story. Neither appears in the 9-section plan (Growth mentions a 'weekly snapshot persistence' only as a preserve-item, not as a surfaced shareable artifact).
- ASSIGN-ACTIVITY -> KID-QUEST AFFORDANCE (LOOP 2 CLOSURE): the plan's Growth/Academy sections add connective HINTS ('feeds the Map/Academy/Care') but never the actual parent action of assigning an activity that becomes the kid's quest — the core of Loop 2.
- KID-ORIGINATED EVENTS IN PARENT FEEDS: Today/Profile activity feeds are planned to be derived ONLY from parent-side data (playLogs/behaviorLogs/milestones). The blueprint requires kid mood check-ins, quest/comic completions, and stars/streak to flow UP into the parent feed (Loops 1 & 3). The plan risks a parent-only feed that breaks the 'two synced worlds' promise.
- DECK'S 'EASY TO KEEP UP' CLUSTER (Day Windows, Smart Reminders, Daily Routines) and 'A TEAM BEHIND EVERY PARENT' cluster (Safety Net, School Handoff, Family Circle) are named system capabilities but are only scattered as preserve-items; the plan has no view that intentionally surfaces Day Windows/Smart Reminders/Daily Routines as the connected 'effortless keep-up' layer, and Safety Net (signs/contacts/crisis script) is not mentioned at all in any of the 9 sections.
- 6-CATEGORY MODEL RECONCILIATION: the deck commits to SIX parent categories (Today, Growth, Academy, Ask Arbor, Care Network, Profile), but the shell plan reshapes to the web-app mock's EIGHT (adding Behaviors + Journal as primary). The plan should reconcile the deck's 6 vs the mock's 8 explicitly so Behaviors/Journal nest under Growth/Today per the deck rather than diverging from the blueprint's IA.
- REALTIME-SYNC HONESTY: the 'Live' pill appears on Today/Profile feeds but the plan flags it only as 'keep static or tie to <7d' — it never connects the pill to the blueprint's actual realtime kid<->parent sync contract, so the differentiator is at risk of being cosmetic.
- MULTIPLE-CHILDREN / KID-ACTIVITY TRACKER ON PROFILE: the deck assigns 'Multiple children, the kid-activity tracker, Arbor Plus & settings' to Profile; the plan's Profile section covers the activity feed and Family Circle but does not treat the multi-child switcher or the kid-activity tracker as the Profile's connected-system obligation.


---
# 3-SESSION COORDINATION CONTRACT


## Shared files (touched by >1 session)

- app/src/index.css — parent reconciliation appends/edits the `.arbor-parent` token block (line ~689+) and may add ONE new `--arbor-*` token for the sapphire topbar band + navy sub-tab pill; the kid/visual session owns the `.arbor-play` blocks (lines ~448-471 base + ~530-560 comic register), `--play-radius`/`--play-radius-lg`/`--comic-*` vars, and the kid design tokens. Both touch this file but in disjoint, clearly-fenced selector regions.
- app/src/lib/i18n.ts — parent adds nav/category labels, nav.title.*/nav.sub.*, nav.short.*, sidebar settings-popover + 'More', today.meta, devscore.stat.*, beh.*, ms.* keys (en+he). Kid session adds aria.kidMode*, KidModeOverlay surface labels, and any kid-surface copy. Same dictionary object, different key namespaces.
- app/src/lib/tokens.ts — typed token mirror consumed by BOTH kit.tsx (parent) and playkit.tsx (kid). Parent reconciliation must NOT alter PlayTone or TONE_INK/TONE_SOFT/BRAND_CONFETTI (kid-owned); kid session must NOT alter parent T.* surface tokens the parent kit relies on. Read-mostly for parent; additive for kid.
- app/src/components/layout/Shell.tsx — parent reshapes the sub-nav pill row, ChildContextHeader accessories, topbar wiring; the KidMode mounts (KidModeProvider wrapper line 180/344, KidModeButton lines 249, KidModeOverlay line 342) are kid-contract regions the parent edits AROUND but must preserve verbatim.
- app/.design-sync/conventions.md — shared design-system contract documenting the two-register rule + tone-vocab sets; either session may append clarifications but the parent must not weaken the parent/kid tone separation, and the kid session must not relax the parent calm-register rule.

## Parent pass MUST NOT touch (kid/visual-owned)

- app/src/components/kidmode/** (KidModeContext.tsx, KidModeOverlay.tsx, parentGate.ts, kidMode.test.ts) — entirely kid-mode-session-owned.
- app/src/components/layout/KidModeButton.tsx — kid-mode-session-owned; parent keeps it mounted in Topbar + the ChildContextHeader accessories row but must not restyle/relocate its internals.
- app/src/components/ui/playkit.tsx — kid-facing primitive set; parent reconciliation never imports from or edits it (parent uses ui/kit.tsx instead).
- app/src/components/ui/ArborMascot.tsx + HeroAvatar.tsx — kid/visual-session assets used by playkit + KidModeOverlay; parent must not restyle.
- index.css `.arbor-play` selector blocks (base ~lines 448-471, comic/Hero-Arcade register ~lines 525-560+) and the `--play-radius`, `--play-radius-lg`, `--comic-ink`, `--comic-line`, `--comic-pop` custom properties + all play-* keyframes/animations — kid/visual-session token zone.
- lib/tokens.ts PlayTone type, TONE_INK, TONE_SOFT, BRAND_CONFETTI exports — kid token source-of-truth.
- The font @import line in index.css (line 1: Fraunces/Nunito/Heebo/Frank Ruhl Libre) — locked shared resource; neither the parent nor any session changes the font set per locked constraint #1. Treat as read-only.
- The kid surface tab components when rendered THROUGH KidModeOverlay (HeroJourneyTab, PracticeHubTab, FeelingsLabTab) — parent may edit these tabs for their parent-route presentation only if it does NOT change kid-overlay behavior; coordinate before touching (they are dual-mounted).

## Kid-mode contracts to preserve

- KidModeProvider must remain the OUTERMOST wrapper inside Shell's return (Shell.tsx line 180 open / line 344 close) so Topbar, KidModeButton, and KidModeOverlay share the isKidModeOpen flag without prop-drilling.
- KidModeOverlay must stay mounted as a SIBLING of the page-shell grid at position:fixed z-index:70 (Shell.tsx line 342), carrying its own `.arbor-play` scope — it must NOT be nested inside `<main className="arbor-parent">` or it would inherit the flat-white parent token overrides.
- KidModeButton must remain mounted in BOTH the Topbar (default pill) and the ChildContextHeader accessories row (compact, line 249) — the design plan explicitly keeps Kid Mode out-of-scope visually but the button + route must survive (UC1 shell preserve-list item).
- The PlayKit tone vocabulary (clay|lav|sky|yellow|pink|peach) and the parent kit/layout tone vocabulary (mint|coral|lav|yellow|pink|sky) are DISTINCT sets — passing a wrong-set tone renders BLANK. The parent sub-tab pills must stay bespoke (inline --arbor-* tokens), never routed through Chip with a clay/peach tone (UC1 plan tone-vocab trap).
- The Escape-key block + hold-to-exit parent gate (3s, parentGate.ts HOLD_MS) in KidModeOverlay is the ONLY kid exit path — parent's Ctrl/Cmd+K search keydown listener (Shell.tsx) and any new topbar key handlers must not interfere with the overlay's capture-phase Escape suppression.
- Kid overlay surfaces are imported UNCHANGED, never forked — if the parent edits HeroJourneyTab/PracticeHubTab/FeelingsLabTab, the change must remain register-neutral so the overlay still renders them under `.arbor-play` correctly.
- The two-register rule in conventions.md (parent = calm green/sapphire kit; child = playful PlayKit + ArborMascot) — parent must never push parent kit styling into kid surfaces and vice-versa.

## Merge risks

- index.css — HIGHEST risk. Parent edits the `.arbor-parent` block + appends a new band/pill token at END of file; kid/visual edits `.arbor-play` blocks earlier in the file. Avoid: parent appends its new token AFTER the existing `.arbor-parent` block (keep the serial-append discipline noted at line 625/687); kid session confines edits to the `.arbor-play`/`--play-*`/`--comic-*` regions. Do NOT have either session reflow whitespace across the whole file (forces a full-file conflict).
- lib/i18n.ts — both sessions add keys to the same en{} and he{} objects. Risk = overlapping insertion points causing line-level conflicts. Avoid: parent inserts under existing nav.*/today.*/beh.*/ms.* clusters; kid inserts under aria.kidMode*/kid-surface clusters; each appends at the END of its own namespace block, never interleaving.
- lib/tokens.ts — risk that parent's --arbor-clay remap (sapphire #2b7fff under .arbor-parent) collides with kid expectations: PlayKit reads --arbor-clay too, but kid renders under .arbor-play OUTSIDE .arbor-parent so it gets the :root sapphire #58a6ff, not the parent remap. Confirm neither session changes the :root --arbor-clay value (locked sapphire) — only the parent's SCOPED override is allowed.
- Shell.tsx — parent's topbar/sub-nav refactor sits physically adjacent to the KidModeButton mount (line 249) and the KidModeProvider/KidModeOverlay lines (180/342/344). Risk of an accidental delete during the accessories-row restructure. Avoid: parent treats lines 180, 249, 342, 344 as immovable anchors and edits only the regions between them.
- navigation.ts — parent's SECTIONS reshape (8 categories) is parent-only, but if the kid session ever adds a kid route to ActiveTab/SECTIONS there is a collision. Coordination: kid mode is an OVERLAY, not a sidebar route — kid session must NOT add entries to SECTIONS or ActiveTab; it stays in KidModeOverlay's own SURFACES registry.
- Dual-mounted surface tabs (HeroJourneyTab/PracticeHubTab/FeelingsLabTab) — both the parent Academy/Grow reconciliation and the kid-overlay reference them. Simultaneous edits risk semantic conflict even without line overlap. Coordination: whoever touches them announces it; prefer register-neutral changes.

## Coordination rules

- File ownership: PARENT session owns navigation.ts, Shell.tsx (excluding the 4 KidMode anchor lines), Topbar.tsx, Sidebar.tsx, MobileNav.tsx, AiRail.tsx, ChildContextHeader.tsx, ui/kit.tsx, and all parent tab/section components. KID/VISUAL session owns components/kidmode/**, KidModeButton.tsx, playkit.tsx, ArborMascot/HeroAvatar, and the `.arbor-play`/`--play-*`/`--comic-*` token zone. The ROS AI-capability-spec session works in docs/spec only and must NOT edit app/src/** at all.
- Token source-of-truth: index.css :root holds the LOCKED sapphire palette (--arbor-clay #58a6ff) — NO session edits :root color values. Parent re-skins ONLY within the scoped `.arbor-parent` block (#2b7fff remap). Kid re-skins ONLY within `.arbor-play`. lib/tokens.ts mirrors these and is additive-only per side (parent never edits PlayTone/TONE_*, kid never edits parent T.* surface tokens).
- Additive-only zones: in i18n.ts each session appends keys at the END of its own namespace cluster (parent: nav.*/today.*/devscore.*/beh.*/ms.*; kid: aria.kidMode*/kid-surface) and never reorders existing keys. In index.css each session edits only its named selector region and appends new tokens at the tail of its own block.
- Immovable anchors in Shell.tsx: KidModeProvider open (≈line 180), KidModeButton compact mount in accessories row (≈line 249), KidModeOverlay sibling mount (≈line 342), KidModeProvider close (≈line 344). Parent edits only the code BETWEEN these anchors; it never deletes, nests, or reorders them. KidModeOverlay must stay OUTSIDE the `.arbor-parent` main.
- No cross-register routing: kid mode stays an overlay with its own SURFACES registry — kid session adds NOTHING to SECTIONS/ActiveTab/TAB_SECTION_FALLBACK; parent's navigation.ts reshape stays parent-only. Tone vocab is never crossed (parent kit = mint|coral|lav|yellow|pink|sky; PlayKit = clay|lav|sky|yellow|pink|peach).
- Dual-mounted tab handshake: before editing HeroJourneyTab.tsx, PracticeHubTab.tsx, or FeelingsLabTab.tsx (rendered in BOTH a parent route and KidModeOverlay), the editing session flags it and keeps the change register-neutral so the other mount is unaffected. Default owner for their parent-route presentation = parent; default owner for kid-overlay behavior = kid.
- Branch hygiene: parent reconciliation lands on claude/uc1-design-impl; kid/visual changes land on their own branch and merge through a coordinated rebase, with index.css + i18n.ts + tokens.ts as the known three-way-merge files to review by hand (region-fenced edits keep these auto-mergeable).