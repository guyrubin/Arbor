---
type: execution-plan
project: Arbor
date: 2026-07-25
status: executing
source: 7-surface AI-excellence assessment + clinical firewall — evidence JSONs in docs/audits/2026-07-25-ai-excellence/
spec: Guy 2026-07-25 — voice cadence to AVM bar, Ask + capture intuitive/natural/augmented
---

# Arbor AI Excellence execution plan (2026-07-25)

Synthesized 42 findings + firewall rulings into 3 sequential waves (18 entries) for one-at-a-time agents on the same tree. No VETOs existed; every CONDITIONS ruling is baked verbatim into the owning entry's acceptance. Key structural moves: (1) per the firewall's FW-NEW-P0 standing order, the two live prod fail-opens (AI-CAP-1 capture crisis fail-open, VC-4 discarded voice escalation payload, plus VC-6 localization) open Wave 1 ahead of all cadence work, even though capture otherwise lives in Wave 2; (2) the LIVE_ENABLED gate (VC-7+AI-V8) lands before any other Live card per the firewall merge-order condition, then the full Live turn-guard sliver (VC-1/2/3/5 with AI-V6 and AI-V9 folded in as ONE implementation) ships inert behind it, then VC-8's crisis output category; (3) EVAL-2 (voice-loop suite) is pulled forward into Wave 1 because AI-V1's acceptance requires it green — it precedes the cadence entry so the red cadence test flips green there; (4) duplicate cards merged: AI-V1+AIR-2 (voice sentence streaming, one implementation), ASK-1+AIR-1 (chat streaming incl. ClaudeVertexProvider streamRawPredict, with the coach-core deterministic eval tier authored in-entry so its own gate is satisfiable); (5) Wave 2 = Ask journey (cadence, UX, capability) then capture intuitiveness (language/typed/provenance, then feel); (6) Wave 3 = routing/latency+telemetry, provider upgrade (guy-gated flip), weak-surface parity and loop closure, then the eval harness and remaining suites. Every entry's acceptance ends with the standard bar: 1760+ tests green, tsc clean, HE/EN+RTL, tokens only, fail-closed safety. Six Guy gates separated out: GD-3 Live flip + provisioning, prod TTS flip, coach model bump, two clinical sign-off bundles (crisis copy/patterns; streamed-delta release), and the comics cross-device retention decision.

## Wave 1

### voice-safety-hotfix — AI-CAP-1, VC-4, VC-6

Fix the two crisis fail-opens live in prod TODAY (sequenced first per the firewall's FW-NEW-P0 standing order, ahead of all cadence work). (a) BehaviorsTab.parseVoice posts spoken transcripts to /api/chat with greedy-regex JSON extraction; a crisis-flagged transcript comes back as escalation markdown, the regex misses, and the raw crisis text lands in an ordinary editable draft with a friendly toast. Replace the call with the existing api.extractLog client; on HTTP 409 with escalationCategory render the renderEscalationMarkdown surface and write ZERO draft fields; clamp/validate all returned fields; delete the regex path. (b) /voice buries the escalation category in the SSE done event and lib/api.ts streamVoice discards done entirely, so voice parents get one generic sentence and the loop resumes listening. Include resourcesMarkdown (renderEscalationMarkdown(match) verbatim) in done, extend streamVoice with an onEvent callback, and on escalation stop the voice loop (never re-listen), persist the resources as an AI ChatMessage, and render a visible blocked state for outputBlocked. (c) Localize the two hardcoded-English safety fallback strings in /voice (escalation :590, blocked :638) with a server-side he/en map keyed on request language, and thread language into the spoken redirect.

**Files:** app/src/components/tabs/BehaviorsTab.tsx, app/src/routes/api.ts, app/src/lib/api.ts, app/src/components/tabs/CoachTab.tsx

**Acceptance:** Crisis fixture transcript produces the escalation UI and zero draft-field writes (test beside confirmCaptureReview.test.ts); no fetch('/api/chat') remains in BehaviorsTab; the 500/network raw-transcript fallback runs only AFTER the escalation branch; an escalation done event produces an on-screen message containing '988' and startListening is NOT re-invoked; outputBlocked done renders a visible blocked state (parity with /chat's renderBlockedOutputMarkdown); resourcesMarkdown is renderEscalationMarkdown verbatim and the CRITICAL_HELPLINE_LITERALS tripwire covers this path; /voice with language:'he' + a self_harm message returns a Hebrew fallback delta, and a flagged HE output returns the Hebrew blocked fallback; HE crisis copy queued for clinical sign-off (gate GG-4) but the fail-closed behavior ships now; 1760+ tests green, tsc clean, HE/EN+RTL, tokens only, fail-closed safety.

### live-enablement-gate — VC-7, AI-V8

Land the Live kill-switch BEFORE any other Live card (firewall merge-order condition): add liveEnabled to ArborConfig (env LIVE_ENABLED, default false) and gate POST /api/live/token on config.liveEnabled && config.geminiApiKey, so setting GEMINI_API_KEY for any other reason can never silently route voice around the screened /voice path. Add GET /api/live/availability computed from config alone (no SDK call, no token mint; available:false unless BOTH flag and key are set — an availability endpoint keyed on the key alone would recreate the hazard). CoachTab probes availability on mount and mints a token only inside toggleVoice, ending the burn of a single-use ephemeral token per tab visit. Put /api/live/token and the upcoming /api/live/turn under the coach meter or a dedicated per-user daily quota via UsageCounterStore. Surface LIVE_MODEL in config/env.ts with a comment on the native-audio upgrade path. Per FW-NEW-P0: pin the systemInstruction (and transcription-on config) into the token's liveConnectConstraints at mint time so a modified client cannot substitute the persona or disable transcription.

**Files:** app/src/routes/api.ts, app/src/config/env.ts, app/src/server/createApp.ts, app/src/components/tabs/CoachTab.tsx

**Acceptance:** Token endpoint returns available:false with key present but LIVE_ENABLED off (test); mounting CoachTab makes zero authTokens.create calls (route spy); toggle still gets a fresh token per session; availability responds <50ms without network; metered 402/429 path covered by test; minted tokens carry the server-pinned systemInstruction and transcription-on constraints; the eventual flag flip to true remains an explicit Guy decision (GG-1), never an env side-effect; 1760+ tests green, tsc clean, HE/EN+RTL, tokens only, fail-closed safety.

### live-turn-guard — VC-1, VC-2, VC-3, VC-5, AI-V6, AI-V9

ONE implementation of the full Live safety spine (AI-V6 folds into VC-1/2/3 per the firewall — the authoritative screen is server-side; any client lexicon copy is only an additional fast path), shipping inert behind LIVE_ENABLED=false. Enable inputAudioTranscription + outputAudioTranscription in live.connect. Build a liveTurnGuard module: all playback buffers per model turn and releases ONLY after (a) the synchronous lexical pass — screenModelOutputLexical extracted to a shared module with the server import path byte-identical — AND (b) the server verdict from new POST /api/live/turn (inside createApiRouter auth + requireChildOwnership; role user → screenForImmediateEscalation, role model → screenModelOutput; always logger.info('live.turn', {requestId, uid, role, chars, verdictCategory}) — NEVER transcript text). playChunk becomes module-private. On stop_crisis: stop() the session BEFORE rendering, append renderEscalationMarkdown verbatim as a persisted ChatMessage, speak a short localized redirect. Turns persist ONLY via appendVoiceUser/applyVoiceDelta/settleVoiceTurn into the existing conversationsCol upsert — any new collection is a VETO-level GDPR allow-list regression. Fail-closed contract (VC-5): a /live/turn network error, non-200, timeout (~3s), or transcription ceasing to arrive is treated as FLAGGED by construction — drop all buffered audio, close the session, show a visible 'Voice switched to standard mode' notice, and degrade to the screened browser loop; no code path resumes Live without a fresh passing verdict. Extract the spoken-coach persona + NON_DIAGNOSTIC_CONTRACT into a shared module feeding both /voice and the Live instruction, with the aiLang-driven Hebrew directive and per-language speechConfig voice (AI-V9); the prompt-level contract is NOT a screening substitute.

**Files:** app/src/lib/geminiLiveClient.ts, app/src/components/tabs/CoachTab.tsx, app/src/routes/api.ts, app/src/safety/outputScreen.ts, app/src/safety/escalation.ts, app/src/lib/voiceTranscript.ts

**Acceptance:** Unit test proves playChunk is unreachable except via the guard; feeding existing outputScreen fixtures ('Noah is autistic and needs therapy.', '3 mg of melatonin') as a turn transcript emits ZERO audio samples and yields the fallback, clean transcript releases audio in order; a self_harm-pattern user transcript returns stop_crisis with resources containing '988', stop() called before the message appends; Live turns land in chatMessages via the COACH-2 reducers and survive settle (conversations stays in CHILD_SUBCOLLECTIONS — export/erase tests green); mocked 500 on /live/turn → stop within one turn, zero further audio, browser loop started, visible notice shown, and a structural test asserts NO catch-and-continue branch exists; transcription absence mid-session closes the session; route test logs both roles with verdicts and no transcript text; Live systemInstruction contains the HE directive when aiLang==='he' and grep shows one source of truth for the spoken persona; everything ships inert behind LIVE_ENABLED; crisis copy EN+HE queued for clinical sign-off (GG-4); 1760+ tests green, tsc clean, HE/EN+RTL, tokens only, fail-closed safety.

### crisis-output-category — VC-8

Add a fourth lexical output-screen category 'crisis' that reuses the compiled escalationCategories pattern lists (self_harm, abuse_or_unsafe_home, caregiver_distress) against MODEL output — closing the gap where harm-normalizing model language passes the voice floor because crisis patterns exist only input-side. Flagged crisis output routes to the crisis fallback + resources, never the generic renderBlockedOutputMarkdown. Apply in /voice, /chat, and the VC-3 /live/turn model-role screen in the SAME PR. The help-directive allow-list (so legitimate 'if you ever feel you might hurt your child, call…' coach language is exempt) is the risk: implement as narrow, fixture-locked frames, and when uncertain FLAG — over-block routes to crisis resources (safe failure), under-block speaks harm aloud.

**Files:** app/src/safety/outputScreen.ts, app/src/safety/escalation.ts, app/src/routes/api.ts

**Acceptance:** Harm-normalizing output is flagged with category 'crisis' and the voice path speaks the crisis fallback + shows resources; the legitimate help-referral sentence and ALL existing clean fixtures remain unflagged (zero regressions in outputScreen.test.ts); allow-list frames are fixture-locked; pattern set + exemption frames queued for clinical sign-off (GG-4); crisis-output scenarios added to evals/voice-loop-v1 (and later coach-core-v1); same-PR coverage of /voice, /chat, and live/turn verified by route tests; 1760+ tests green, tsc clean, HE/EN+RTL, tokens only, fail-closed safety.

### voice-eval-suite — EVAL-2

Pulled forward from the evals wave because it is the named acceptance instrument for the cadence entry that follows (AI-V1 may only ship with this suite green). Author evals/voice-loop-v1.eval.json with a deterministic tier that runs against the REAL /voice handler with a stub provider in vitest: EN happy path (2-4 sentences, no markdown/bullets/emojis), HE reply register, diagnosis-bait, escalation input → speak-safe fallback + on-screen resources, HE escalation → Hebrew fallback (VC-6), long-answer cadence, benign follow-up, mid-stream flagged-sentence (prior sentences + fallback only), and the Live turn-guard scenarios (crisis closes session <1 turn; screening-endpoint-down → visible degrade). Extract CoachTab's sentence-splitter into a pure lib function and add the cadence unit test asserting first enqueueSpeak fires before stream-done — red against today's single-delta /voice, flipping green with the next entry. Promote voice-smoke.mts to an eval:voice-live npm script with enforced budgets (firstTokenMs<2000, totalMs<8000, first-sentence-available<2500ms) appending {ts, model, firstTokenMs, totalMs} to evals/voice-loop-v1.results.jsonl.

**Files:** evals/voice-loop-v1.eval.json, app/src/routes/api.ts, app/src/components/tabs/CoachTab.tsx, app/scripts/voice-smoke.mts, app/package.json

**Acceptance:** Deterministic tier runs in npm test against the real /voice handler; the cadence unit test exists and is documented as red-until-cadence-entry; eval:voice-live produces a trendable append-only results row per run; suite schema is EVAL-1-compatible (pinned judge, safety-trip scenario present); 1760+ tests green, tsc clean, HE/EN+RTL, tokens only, fail-closed safety.

### voice-cadence — AI-V1, AIR-2, AI-V3, AI-V4, AI-V5

The cadence overhaul on the browser-fallback path prod actually runs, unlocked by the safety sliver above. AI-V1+AIR-2 are ONE implementation (firewall merge): /voice splits the model stream at sentence boundaries, runs the output screen on the CUMULATIVE alias-RESTORED text (after createStreamRestorer, never a sentence in isolation) at each boundary, and emits sentence N as its own delta only after its screen passes; on any flag stop the stream and emit the existing calm fallback + outputBlocked; when ENABLE_OUTPUT_SAFETY_CLASSIFIER=true keep today's full-buffer behavior (config-gated); escalation path at :587-593 stays byte-identical; correct the stale lib/api.ts token-streaming comment. AI-V3: recognition errors restart listening (backoff + max-retry circuit breaker) for recoverable errors; 'not-allowed' stops voice with the mic-permission toast; wire onEnd so the mic auto-cycles truthfully. AI-V4: thread lang through speakText→startBrowser, set utterance.lang from getAiLanguage, pick the best matching voice via getVoices()+voiceschanged, rate 1.0; collapse the double gate by dropping VITE_TTS_PROVIDER so the GET /api/tts configured probe alone flips the engine — zero change to the server screen seam. AI-V5: prefetch sentence N+1's audio while N plays (single-owner interruption semantics kept); /voice mints a short-TTL HMAC (server secret over sha256(exact text)+lang+expiry, constant-time verify) per screened sentence and /api/tts skips only the model re-screen on a valid token — screenModelOutputLexical still runs UNCONDITIONALLY on every /api/tts call; scale the 1500ms TTFB watchdog to fetch-start when a prefetched buffer exists.

**Files:** app/src/routes/api.ts, app/src/lib/api.ts, app/src/components/tabs/CoachTab.tsx, app/src/lib/speech.ts, app/src/lib/voice.ts, app/src/lib/naturalVoice.ts, app/src/config/env.ts, app/src/server/tts.ts, app/src/safety/outputScreen.ts

**Acceptance:** SSE for a 3-sentence reply contains >=3 delta events and the first delta arrives <1.5s after sentence 1 completes; route test: flagged sentence 2 emits sentence 1 + calm fallback and the flagged text appears in NO SSE frame; classifier-on config keeps full-buffer behavior (test); escalation/abort behavior unchanged; simulated 'no-speech' → a new startDictation call (spy) with truthful phase, 'not-allowed' → voice off + toast, circuit-breaker prevents infinite restart; HE utterance has lang='he-IL' and an he voice when available (mocked getVoices); engine flips to 'natural' with ONLY TTS_PROVIDER=google set server-side; UnsafeTtsOutputError 422 route test stays green; inter-sentence gap <250ms with a mocked 800ms synth; /api/tts with absent/invalid/expired token or one-char-altered text is FULLY screened (route test); no engine downgrade during a normal multi-sentence answer; evals/voice-loop-v1 green including the mid-stream-flag scenario and the cadence unit test now passing; 1760+ tests green, tsc clean, HE/EN+RTL, tokens only, fail-closed safety.

### voice-barge-in-ui — AI-V2, AI-V7

Barge-in on both paths plus the missing voice surface. Fallback: while speaking, chip tap = INTERRUPT not off — flush ttsQueueRef, stopSpeaking(), abort any open voice stream, finalizeVoiceAiTurn(), immediately startListening(); a second tap during listening turns voice off. Live: retain scheduled AudioBufferSourceNodes, and on serverContent.interrupted stop() them all and reset playHead — implemented INSIDE/BEHIND the liveTurnGuard from the turn-guard entry, never as a second direct caller of playChunk (which stays module-private). UI: a calm bottom-sheet voice overlay owned by voicePhase !== 'off' — phase-animated orb (listening = soft pulse driven by real mic level via AnalyserNode on a shared getUserMedia stream; thinking = slow shimmer; speaking = waveform), live captions dir="auto" for both the interim user transcript (interimResults=true + new onInterim handler in lib/speech.ts) and the streaming answer, tap-orb = barge-in, X = end; prefers-reduced-motion = static phase states; the chip remains the sole entry point (no new capture path); captions render only the parent's own words + already-screened /voice output.

**Files:** app/src/components/tabs/CoachTab.tsx, app/src/lib/geminiLiveClient.ts, app/src/lib/speech.ts

**Acceptance:** Speaking→tap reaches 'listening' with mic active <300ms; unit test on a Live message sequence {data..., interrupted} asserts all retained sources stopped and playHead reset, routed via the guard (no new direct playChunk caller — guard unreachability test still green); double-tap fully stops (parity with today); all four phases visually distinct without reading text; interim user words render <500ms after speech starts; axe clean, aria-live on captions, reduced-motion snapshot test; green calm register, full RTL mirroring; 1760+ tests green, tsc clean, HE/EN+RTL, tokens only, fail-closed safety.

## Wave 2

### ask-cadence — ASK-1, AIR-1

ONE implementation (firewall merge) of real /chat streaming. Phase 1 (no gate): replace the character-counter statuses with 3 localized, honest stages driven by real server milestones (reading {name}'s memory → checking sources → shaping today's plan) plus an immediate locally-rendered acknowledgment bubble; delete the post-hoc fake typewriter for contract answers. Phase 2 (behind clinical gate GG-5): implement streamRawPredict SSE in ClaudeVertexProvider (the seam that makes prod streaming possible at all) and stream the contract's prose text field as sentence-level delta events — screenModelOutputLexical runs cumulatively on the alias-RESTORED text per delta (screen AFTER the stream restorer, matching the /voice ordering, since the NAME_SUBJECT floor assumes restored names); the semantic classifier (when enabled) still gates the done payload, and on a done-time flag the client RETRACTS the streamed bubble and replaces it with renderBlockedOutputMarkdown; structured panels stay gated at done; the escalation pre-screen at :328 is untouched. Author the deterministic tier of evals/coach-core-v1 here (zod parse, sourceCardsUsed subset, todayPlan 1-3, no verdict strings) so this entry's acceptance condition is satisfiable — EVAL-5 completes the judge dims in Wave 3.

**Files:** app/src/routes/api.ts, app/src/context/ArborContext.tsx, app/src/components/ui/TypewriterMarkdown.tsx, app/src/components/tabs/CoachTab.tsx, app/src/ai/claudeVertexProvider.ts, app/src/safety/outputScreen.ts, evals/coach-core-v1.eval.json

**Acceptance:** p50 time-to-first-visible-words <2.5s; no English status strings in HE; SSE-level test: a response containing 'Mia has autism' mid-stream NEVER renders the flagged sentence; retract-and-replace on done-time classifier flag is tested; generateJsonStream yields >1 chunk (integration test); escalation pre-screen and blocked-fallback payload shapes unchanged; coach-core-v1 deterministic tier green before merge; HE/RTL deltas render correctly; 1760+ tests green, tsc clean, HE/EN+RTL, tokens only, fail-closed safety.

### ask-journey-ux — ASK-2, ASK-3, ASK-7, ASK-8, ASK-9

The 22:30 stressed-parent flow fixes, all firewall-PASS. Sticky composer: same single element renders hero-position on a fresh thread and docks position:sticky bottom-0 once the thread has a user turn (COACH-4 single-input invariant holds — one textarea in the DOM, voice/photo chips travel with it). Answer reorder: parentScript ('Say this') and todayPlan lead; hypotheses collapse into a 'Why this might be happening' disclosure; frameRouting panel removed from the parent render entirely (kept in the contract for telemetry/evals); also fix the stale 'risk' comment at CoachAnswerCards.tsx:125 (FW-NEW-P0 P2). Orientation: delete WELCOME_MESSAGE, initialize chatMessages to [], convert all length<=1 guards to hasUserTurn/hasAiTurn predicates, keep mascot + scenarios as the single orientation block. Errors: single calm role=alert retry card only — no appended error bubbles with Firestore/index/provider internals, error-only turns excluded from the conversation upsert, Retry resends without duplicating the user question. Lens: demote the lens block below the fast-start scenarios into a single 'Perspective: Integrated · change' affordance, full radiogroup one tap away, persisted selection + seedCoach lens steering intact.

**Files:** app/src/components/tabs/CoachTab.tsx, app/src/context/ArborContext.tsx, app/src/components/coach/CoachAnswerCards.tsx, app/src/components/coach/CoachAnswerCards.test.ts

**Acceptance:** On 375x812, after an answer settles, input + send are visible with zero scrolling and exactly one textarea exists in the DOM; first mobile screenful of any contract answer = script + plan; frames never visible; fresh thread shows exactly one orientation block with persistence/title-derivation/TrustSafetyBar behavior identical; kill-network mid-question → the question appears once + one calm retry card, the saved conversation contains no error text, error copy has no stack/infra vocabulary in either language; lens change reachable <=2 taps, arrow-key a11y + RTL preserved, 'aligned with' chip still shows for non-default lens; CoachAnswerCards.test.ts updated and green; 1760+ tests green, tsc clean, HE/EN+RTL, tokens only, fail-closed safety.

### ask-capability — ASK-4, ASK-5, ASK-6

Anticipation, language parity, and visible memory. followUps: add a zod-capped field (<=3 short strings, localized via languageDirective) to coachResponseSchema, rendered as the chips with the static trio as fallback — and CRITICALLY appended to renderCoachResponse so screenModelOutput covers them (a rendered-but-unscreened field would be the first bypass of the output screen); Council with empty input re-asks lastUserText, disabled with a hint only when no prior turn exists. i18n: move WELCOME/status/402/cancel/error injected strings to t() keys; add displayText to ChatMessage so a tapped Hebrew scenario chip shows its localized label as the user bubble while the canonical EN prompt goes to the model (approved hardMomentSurface pattern). Memory visibility: server adds approvedMemoryFactsUsed as an integer COUNT only; CoachAnswerCards renders 'Grounded in {n} facts you approved · manage' deep-linking to the memory route; new pending memoryReviewItems render a one-line 'Arbor suggests remembering something — review' chip; nothing auto-saves, queue mechanics and the propose seam untouched, zero memory CONTENT rendered in-thread.

**Files:** app/src/routes/api.ts, app/src/types.ts, app/src/contracts/coach.ts, app/src/components/tabs/CoachTab.tsx, app/src/context/ArborContext.tsx, app/src/components/coach/CoachAnswerCards.tsx, app/src/lib/i18n.ts

**Acceptance:** Chips differ across two different questions and arrive in uiLang; test asserts renderCoachResponse output contains the followUps (screen coverage); Council tapped after an answer visibly convenes on that question; a full HE journey (fresh thread → scenario tap → answer → follow-up → cancel → error) shows zero English and user bubbles match what was tapped; grounded answers show the count row, a proposal turn shows the review chip once, no percentage/confidence wording in the footer, zero memory content in-thread; followUps eval case added to the coach-core suite; 1760+ tests green, tsc clean, HE/EN+RTL, tokens only, fail-closed safety.

### capture-language-typed — AI-CAP-2, AI-CAP-3, AI-CAP-4, AI-CAP-8

Make 'telling Arbor' real in both languages and both modalities, all through the ONE extraction seam AI-CAP-1 already hardened. HE end-to-end: callers pass startDictation(handlers, uiLang==='he' ? 'he-IL' : 'en-US'); add language to the extractLog payload and a Hebrew directive to the /extract-log prompt (mirroring /chat's languageDirective). Typed capture: on Enter (>~25 chars) in the Behaviors capture bar and QuickLogModal, call api.extractLog, prefill the full draft, arm needsReview, open ConfirmCaptureReview directly; an HTTP 409 with escalationCategory on the TYPED path renders the escalation surface with ZERO draft-field writes (the ApiError must not fall through to sentence-into-trigger — same contract as AI-CAP-1); extraction-filled drafts carry provenance 'ai-draft', never 'text'; empty response gets a visibly-editable neutral placeholder; short inputs keep today's behavior; extraction failure degrades to today only after the escalation branch. Coach handoff: onCreateLog routes through the review seam — captureOpen set, needsReview armed, source 'ai-draft' with factual wording ('Drafted by Arbor from your chat — check every field'), form scrolled into view; the only write path is confirmReview. Taxonomy: one shared module with the canonical six types + i18n keys + mapLabelToType, free labels preserved in notes, both selects import from it, validation/clamping after every extraction.

**Files:** app/src/lib/speech.ts, app/src/components/tabs/BehaviorsTab.tsx, app/src/components/tabs/CoachTab.tsx, app/src/components/overview/QuickLogModal.tsx, app/src/components/overview/ConfirmCaptureReview.tsx, app/src/routes/api.ts, app/src/lib/api.ts, app/src/context/ArborContext.tsx

**Acceptance:** Unit test asserts rec.lang='he-IL' when passed; /extract-log prompt snapshot contains the Hebrew directive when language==='he'; spoken/typed HE yields HE trigger/response/notes, RTL-correct; messy typed sentence + Enter yields a fully-prefilled review card in <=1 model round-trip and confirm writes exactly one log via confirmReview only; typed-path 409 renders escalation with zero draft writes (test); coach→Create-log lands with the form visible, populated, review-gated, and the provenance line states the factual ai-draft source with no confidence wording (CODEX-7 test extended); any extraction result renders a valid visible selection in both forms, 30-day counts and the type filter group correctly, no duplicated option literals; EN+HE keys added; 1760+ tests green, tsc clean, HE/EN+RTL, tokens only, fail-closed safety.

### capture-feel — AI-CAP-5, AI-CAP-6, AI-CAP-7

Close the loop on how capture FEELS. Review honesty: extend the single shared ConfirmCaptureReview (no fork) with intensity/context/duration rows — exactly the fields the AI guesses most aggressively — with inline correction in place (context as the existing 4-chip row, intensity as a 1-5 stepper, text rows tap-to-edit) and an optional factual 'from your note' provenance label (no certainty wording). Dictation feel: speech.ts opts overload with interimResults=true, continuous=true, onInterim callback, and a ~4-5s silence-finalize timer plus manual stop; BehaviorsTab renders the interim transcript live in the capture area (calm register, --arbor-muted); CoachTab opts out or adopts intentionally. Post-confirm handoff: after a gated confirm, ONE dismissible non-blocking strip 'Want a next step for this moment? Ask Arbor' calling seedCoach with a prompt built from the confirmed log and source 'post-capture' — never auto-send, once per confirm, no change to the write path.

**Files:** app/src/components/overview/ConfirmCaptureReview.tsx, app/src/components/tabs/BehaviorsTab.tsx, app/src/components/overview/QuickLogModal.tsx, app/src/components/overview/confirmCaptureReview.test.ts, app/src/lib/speech.ts, app/src/context/ArborContext.tsx

**Acceptance:** A voice/ai-draft review shows all 7 fields; changing intensity/context inside review updates the confirmed write without leaving the review; both consumers render the same component — confirmCaptureReview.test.ts extended to assert intensity/context presence and no confidence wording; spoken words appear <500ms after being said and a 1.5s pause does NOT end capture; manual stop or silence timeout finalizes and hands the full transcript to extraction; CoachTab behavior unchanged; confirming shows exactly one optional CTA, tap lands in Coach prefilled (not sent) with coach_seed source='post-capture', dismiss leaves no residue; reduced-motion and RTL safe for chips/stepper; EN+HE copy; 1760+ tests green, tsc clean, HE/EN+RTL, tokens only, fail-closed safety.

## Wave 3

### routing-latency — AIR-3, AIR-5, AIR-6, AIR-7, AIR-9, EVAL-7

Remove the invisible seconds. Thinking budgets: thinkingBudget 0 for analysis_structured and voice streamText in both Gemini providers, small/dynamic retained for creative/coach routes (touching the semantic classifier call is acceptable — it is default-OFF and fails open; the lexical floor is untouched). Latency telemetry (EVAL-7): capture t0/firstChunkMs/totalMs at the modelRouter streamText/generateJson/generateJsonStream seams, into usage events + p50/p95 in the usageRollup — the data source every cadence budget reads. Today's Focus: dedicated lightweight endpoint (analysis route, 2-field schema, daily cache, excluded from the coach meter but inside the hourly AI quota) whose output PASSES screenModelOutput before return with a route test, prompt embeds NON_DIAGNOSTIC_CONTRACT, focus text is observation/next-step only, cache serves only screened payloads — the firewall condition that keeps this from becoming the first unscreened parent-facing generative surface. TTS metering: /api/tts off the model-call quota onto a character-based daily TTS cap; screenModelOutputLexical stays UNCONDITIONAL on every call; 'already-screened' provenance proven only via the Wave-1 HMAC token, never a client-asserted flag. Middleware: collapse quota/meter increments to one round-trip each, run lookups concurrently, log middleware duration. Deadlines: per-route AbortSignal budgets (coach/council 45s, analysis/extract 15s, voice 20s, image 60s) tied to req close, calm parent-register error on expiry, retry backoff skipped under 40% remaining budget.

**Files:** app/src/ai/modelRouter.ts, app/src/ai/usage.ts, app/src/ai/usage.test.ts, app/src/ai/claudeVertexProvider.ts, app/src/ai/modelRetry.ts, app/src/hooks/useTodaysFocus.ts, app/src/routes/api.ts, app/src/server/createApp.ts, app/src/server/quotaStore.ts, app/src/server/aiQuota.ts, app/src/server/entitlements.ts, app/src/server/tts.ts

**Acceptance:** /extract-log p50 server time drops (log-verified before/after) with no extract-log test regressions; every usage event carries totalMs (and firstChunkMs for streams), usage.test.ts green; Today card load does not change X-Coach-Remaining, focus route output-screen route test green, p50 <3s, copy unchanged EN+HE; a 10-minute continuous voice session never 429s on /tts, X-AI-Quota-Remaining unaffected by spoken sentences, UnsafeTtsOutputError test green; pre-handler middleware p50 <70ms with aiQuota/entitlements tests unchanged; stubbed never-resolving provider returns a structured calm error within the route budget and client abort cancels the upstream call (signal.aborted spy); 1760+ tests green, tsc clean, HE/EN+RTL, tokens only, fail-closed safety.

### provider-upgrade — AIR-4, AIR-8

Model currency + seam hygiene. AIR-4 (prod flip = Guy gate GG-3): with streamRawPredict already landed in Wave 2, bump the default VERTEX_MODEL_CHAT to the current Claude Sonnet available on Vertex EU regions; firewall conditions — coach-core-v1 + eval:safety + the hard-moment suite must re-run GREEN against the NEW resolved model id before any prod flip, and evals/pinned-models.json is refreshed in the SAME PR; the EU-residency policy_denied gate stays green. AIR-8: resolve the dead CapabilityRegistry — wire TTS through registry.get(capability, provider).execute as the first real consumer, with the hard condition that screenAndSynthesizeSpeech remains the ONLY publicly reachable synthesis path (registry.execute must not expose unscreened synthesizeSpeech to any route) and not_configured fails closed; or delete the registry and correct the misleading comment.

**Files:** app/src/ai/claudeVertexProvider.ts, app/src/config/env.ts, app/src/server/createApp.test.ts, evals/pinned-models.json, app/src/ai/capabilities/registry.ts, app/src/server/tts.ts, app/src/server/createApp.ts

**Acceptance:** routeDecision test asserts the new model id; all three suites green against the new resolved model id before the flip, pinned-models.json refreshed same PR; policy_denied still thrown for non-EU config; prod env flip + quality sign-off remain Guy's (GG-3); either zero CapabilityRegistry references outside capabilities/ or at least one production path resolves through registry.get with not_configured failing closed and no unscreened synthesis reachable (tests); 1760+ tests green, tsc clean, HE/EN+RTL, tokens only, fail-closed safety.

### surface-parity — AIX-S1, AIX-S2, AIX-S4, AIX-S7

Kill the HE/EN parity breaks on the weak surfaces. ArborVision: move every string to i18n keys with HE translations, send language: getAiLanguage() in the api.vision payload, and append the /digest-pattern languageDirective to both observe and document prompts server-side. Speech scorer: derive recognition language exactly as CoachTab does (aiLang → 'he-IL'/'en-US'); since SOUND_LIBRARY targets are EN, suppress the auto-listen verdict path entirely for non-EN sessions and fall to the parent-scoring floor with honest copy — the machine may never assert a 'sounds different' verdict it provably cannot hear. Coach seeds: buildCoachSeed(i18nKey, params) helper converting all ~10 hardcoded-English seedCoach call sites (DailyPlay, LanguageLab, Overview, Milestones, Plans, Vision), locked by a grep-test in the hardMomentSurfaces.test.ts style that fails on raw-English template literals inside seedCoach prompts. ComicsTab: convert the no-hero invite and trust/safety footer to the file's he? pattern — the never-a-real-photo and provenance-watermark claims must survive verbatim in meaning.

**Files:** app/src/components/coach/ArborVision.tsx, app/src/routes/api.ts, app/src/lib/api.ts, app/src/lib/i18n.ts, app/src/components/practice/SpeechCoachTab.tsx, app/src/lib/speechScorer.ts, app/src/components/tabs/DailyPlayTab.tsx, app/src/components/tabs/LanguageLabTab.tsx, app/src/components/tabs/OverviewTab.tsx, app/src/components/tabs/MilestonesTab.tsx, app/src/components/tabs/PlansTab.tsx, app/src/components/tabs/ComicsTab.tsx

**Acceptance:** HE session renders the Vision modal fully in Hebrew (RTL) with Hebrew observations/tryToday/summary, server test asserts the Hebrew directive in the /vision prompt (mirroring the /digest test); an HE session never renders an en-US mis-transcript verdict — unit test asserts recognition lang selection and autoResult stays null for non-EN, parent scoring unaffected; HE seeds Hebrew prompts on every listed surface and the grep-test locks the pattern with EN behavior unchanged; HE Comics bookends render in Hebrew with the safety claims intact, i18n test covers the no-hero branch; 1760+ tests green, tsc clean, HE/EN+RTL, tokens only, fail-closed safety.

### surface-loops — AIX-S3, AIX-S5, AIX-S6

Close the dead-ended loops. Vision: thread the handoffNote through CoachTab's onGoHandoff into a consult-composer prefill seam (parent-editable; prefill is not consent — no share/send without the existing explicit send act), and add a per-item 'Save to {name}'s memory' CTA on suggestedMemory calling the existing POST /memory/:childId/propose so items land ONLY in the pending-approval queue (nothing auto-approves); both CTAs i18n'd. Comics: honesty layer first — when rehydrateSavedPages returns [], the shelf badge/aria switch to 'Rebuild this book' (HE+EN), 'Read again' only when all pages are cached; then durability — IndexedDB read-through page store (~30 pages, LRU, keyed per child, device-local only, NEVER uploaded/synced), purged on child erase and sign-out so a GDPR-erased child leaves no comic pages on the device, with a test/lint proving no network write reads the store. Digest: 'Make this today's step' CTA on the tryThisWeek card calling the existing acceptTodayAction seam with digest provenance, present only when digest.generated==='ai' (fallback digests keep the plain card).

**Files:** app/src/components/coach/ArborVision.tsx, app/src/components/tabs/CoachTab.tsx, app/src/routes/api.ts, app/src/components/tabs/ComicsTab.tsx, app/src/lib/heroComics.ts, app/src/lib/sceneCache.ts, app/src/components/tabs/WeeklyTab.tsx, app/src/components/overview/TodayRecommendation.tsx, app/src/context/ArborContext.tsx

**Acceptance:** 'Use in a handoff' opens the consult composer with the note prefilled (CoachTab mount test asserts the argument is consumed); proposing a memory item creates a pending memory event visible in ChildMemory with parent approval unchanged; reopening a saved book after a full page reload renders all pages with ZERO /generate-comic calls (network assert); cold state never shows 'Read again'; child erase and sign-out purge the IndexedDB store (test); no network write reads the store (test/lint); quota errors degrade to today's behavior; digest CTA creates an actionLoop with digest provenance and is absent when generated==='fallback'; HE+EN copy throughout; 1760+ tests green, tsc clean, HE/EN+RTL, tokens only, fail-closed safety.

### eval-harness — EVAL-1, EVAL-4, EVAL-6, EVAL-8

Build the enforcement seam every CONDITIONS ruling in this slate cites. EVAL-1: app/scripts/acceptance-eval.mts + 'check:acceptance' script — offline tier (always, CI) validates every evals/*.eval.json against a suite schema (pinned judgeModel, semver, hard-safe rubric gate, >=5 scenarios including one safety-trip) and runs each suite's deterministic contract assertions; live tier (when Vertex ADC/ANTHROPIC_API_KEY present) invokes the judge runner per suite, failing on any safe===false or pass-rate below the suite passBar; wired into arbor-ci.yml after eval:safety. EVAL-4: generic app/scripts/eval-judge.mts — loads a suite, runs each scenario through the REAL route with the approved-fixture pattern, one judge call on the pinned judgeModel routed through claudeVertexProvider on the existing Vertex ADC (ANTHROPIC_API_KEY as alternative), appends {ts, suite, version, judgeModel, resolvedRouteModel, perScenario, passRate} to append-only results.jsonl. EVAL-6: src/ai/prompts.ts with named prompt builders extracted from api.ts + PROMPT_VERSIONS {version, sha256} registry, hash-guard test in the contentHash pattern ('prompt changed — bump version + re-run its eval suite'), promptVersion stamped into usage events and results rows, NON_DIAGNOSTIC_CONTRACT gets its own version, stale-suite WARN in check:acceptance. EVAL-8: evals/pinned-models.json + drift check in acceptance-eval.mts, resolved model id recorded in every usage event and results row, safety-eval's stale-model check reads the pinned file instead of a hardcoded regex.

**Files:** app/scripts/acceptance-eval.mts, app/scripts/eval-judge.mts, app/scripts/safety-eval.mts, app/package.json, .github/workflows/arbor-ci.yml, app/src/ai/prompts.ts, app/src/ai/usage.ts, app/src/routes/api.ts, app/src/contracts/coach.ts, evals/pinned-models.json, evals/coach-hardmoment-seed-v1.eval.json

**Acceptance:** A deliberately malformed suite (unpinned judge, no safety-trip scenario) fails CI; check:acceptance exits non-zero when any deterministic contract fails, with per-suite verdict lines, offline tier <60s; 'npm run eval:judge -- coach-hardmoment-seed-v1' produces a results row with 6 scenario verdicts, exits non-zero on safe===false or escalationVerbatim<1.0, second run appends never overwrites; editing a prompt template without bumping its version fails npm test; every usage event and eval result row carries promptVersion and the resolved model id; bumping VERTEX_MODEL_ANALYSIS without refreshing pinned-models.json fails check:acceptance; alias→snapshot resolution asserted in modelRouter.test.ts; 1760+ tests green, tsc clean, HE/EN+RTL, tokens only, fail-closed safety.

### eval-suites — EVAL-3, EVAL-5

The two remaining capability suites, now runnable under the harness. capture-extract-v1: >=8 scenarios (EN happy path, vague-duration default, multi-behavior→one log, parent-emotion-heavy→neutral notes, escalation-bait→deterministic 409 with escalationCategory — permanently covering the AI-CAP-1 regression, HE description→faithful HE extraction, sarcasm/ambiguity, empty→400); deterministic tier runs the real route handler with a stub provider in npm test; judge dims fidelity >=0.8, neutrality =1.0 HARD (observation language, no labels), one-log =1.0. coach-core-v1 completion: extend the Wave-2 deterministic tier to the full 7 scenarios (EN, HE naturalness, diagnosis-bait, lens fidelity, memory-grounding, safety-trip, day-0) plus judge dims (groundedness >=0.8, warmth >=0.7, actionability >=0.8, noDiagnosis =1.0 hard, heNaturalness >=0.7) and followUps scenarios; deterministic assertions include sourceCardsUsed ⊆ offered card ids (turning today's silent citation drop into a measurable hallucination rate) and the sharpened firewall condition: riskLevel is contract-internal ONLY — it may tier escalation prominence but its VALUE never renders as text in any parent-facing field and never reaches an export (clinicalFirewall wave3/wave4 tests remain the export guard).

**Files:** evals/capture-extract-v1.eval.json, evals/coach-core-v1.eval.json, app/src/routes/api.ts, app/src/contracts/coach.ts, app/src/safety/outputScreen.ts

**Acceptance:** Both suites validate under the EVAL-1 schema and their deterministic tiers run in npm test; a prompt edit that makes extraction emit a label ('sounds like ADHD-driven behavior') fails the judge tier; a prompt change that cites unoffered cards fails deterministically; riskLevel-never-rendered assertion green; escalation-bait yields 409 deterministically; results.jsonl trends warmth/groundedness per resolved model id; 1760+ tests green, tsc clean, HE/EN+RTL, tokens only, fail-closed safety.

## Guy gates

- **GG-1-GD3-LIVE** — Flip LIVE_ENABLED=true in prod (the GD-3 unlock): requires Gemini Live API provisioning on the account and a choice of native-audio LIVE_MODEL (the env default gemini-2.0-flash-live-001 is the older half-cascade voice). Only after Wave 1 entries 2-6 are green; the flag flip is a deliberate decision, never an env side-effect — Wave 1 ships everything inert behind it.
- **GG-2-TTS-PROD** — Enable the Cloud Text-to-Speech API on the prod GCP project and set TTS_PROVIDER=google (single server flag after the Wave-1 double-gate collapse; same-project ADC, no new vendor/DPA per server/tts.ts). This is the default-on flip that gives every parent the neural voice instead of robotic speechSynthesis.
- **GG-3-MODEL-BUMP** — Approve the prod coach model upgrade (AIR-4): sign off model quality and flip the prod VERTEX_MODEL_CHAT env var — only after coach-core-v1 + eval:safety + hard-moment suites re-run green against the new resolved model id and evals/pinned-models.json is refreshed in the same PR.
- **GG-4-CLINICAL-CRISIS** — Route clinical sign-off for: (a) EN+HE crisis fallback copy and spoken redirects (Wave 1 hotfix + Live sliver); (b) Live crisis trigger thresholds; (c) the VC-8 crisis output-category pattern set and help-directive allow-list frames; (d) the AI-CAP-1 escalation surface on the capture path. Fail-closed behavior ships immediately; copy/threshold wording awaits this sign-off.
- **GG-5-CLINICAL-STREAMING** — Approve sentence-level streaming of lexically-screened deltas on /voice (Wave 1 cadence) and /chat Phase 2 (Wave 2 ask-cadence) — the clinical-gated change from full-buffer-then-screen to cumulative-screened incremental release. Phase 1 (honest statuses, no typewriter) ships without it.
- **GG-6-COMICS-RETENTION** — Retention decision: Wave 3 ships comic-page art persistence as device-local IndexedDB only (purged on erase/sign-out, never uploaded). Decide whether to later grant the Firebase Storage layer for cross-device durability — until then, the shelf stays honest ('Rebuild this book') on any other device.
